module.exports = function(app) {

/*
Recebe o BBSSOToken e verifica se tem algum registro no redis (verifica se a conexão está valida e retorna as informações do usuaŕio)

*/

  // app.get('/', (req, res, next) => {
  //     res.render('index')
  // //  res.status(201).json({success: false, msg: 'Usuario não encontrado'});
  // })
}
